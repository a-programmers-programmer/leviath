flowchart LR
    gather{{gather}}
    analyze{{analyze}}
    summarize(summarize)
    summary(summary)
    dig(dig)
    dig_workers[["researcher (sub-agent)"]]
    gather --> dig
    gather --> analyze
    gather -. dead_end .-> summarize
    analyze --> gather
    analyze --> summarize
    summarize --> summary
    dig ==>|×3 workers| dig_workers
    dig --> analyze
    classDef entry stroke-width:3px
    class gather entry
