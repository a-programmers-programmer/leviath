flowchart LR
    scope(scope)
    split(split)
    gather_worker(gather worker)
    build(build)
    present(present)
    scope --> split
    scope -. dead_end .-> build
    split ==>|×30 workers| gather_worker
    gather_worker ==>|merge| build
    build --> present
    classDef entry stroke-width:3px
    class scope entry
